configs:
  traefik-dynamic.yaml:
    content: |
      http:
        routers:
          wg-easy:
            rule: Host(`vpn.${INTERNAL_DOMAIN}`)
            entrypoints:
              - websecure
            service: wg-easy

        services:
          wg-easy:
            loadBalancer:
              servers:
                - url: http://${VM_IP}:51821
  traefik.yaml:
    content: |
      api:
        dashboard: true
        insecure: true

      providers:
        docker:
          exposedByDefault: false
          network: proxy
        file:
          filename: /etc/traefik/traefik-dynamic.yaml

      entryPoints:
        web:
          address: ":80"
          http:
            redirections:
              entrypoint:
                to: websecure
                scheme: https
        websecure:
          address: ":443"
          http:
            tls:
              certResolver: "letsencrypt"

      certificatesResolvers:
        letsencrypt:
          acme:
            email: "afonso.antunes@live.com.pt"
            storage: "/letsencrypt/acme.json"
            caServer: "https://acme-v02.api.letsencrypt.org/directory"
            dnsChallenge:
              provider: cloudflare  # or your DNS provider
              resolvers:
                - "1.1.1.1:53"
                - "8.8.8.8:53"

      log:
        filePath: /var/log/traefik/traefik.log
        format: json
        level: DEBUG

      accessLog:
        filePath: /var/log/traefik/access.log
        format: json

      serversTransport:
        insecureSkipVerify: true

services:
  traefik:
    container_name: traefik
    image: docker.io/traefik:v3.7.12@sha256:9c2a54d87f76f5c2f5f2682c68394af92fb12c0a2686798d6462a3f84bd78eaf
    restart: unless-stopped
    env_file:
      - traefik.env
    ports:
      - 80:80
      - 443:443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - letsencrypt:/letsencrypt
      - /var/log/docker/traefik:/var/log/traefik
    configs:
      - source: traefik.yaml
        target: /etc/traefik/traefik.yaml
      - source: traefik-dynamic.yaml
        target: /etc/traefik/traefik-dynamic.yaml
    logging:
      driver: "json-file"
      options:
        max-size: "500m"
        max-file: "2"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.dashboard.rule=Host(`vpn.proxy.${INTERNAL_DOMAIN}`)"
      - "traefik.http.routers.dashboard.entrypoints=websecure"
      - "traefik.http.routers.dashboard.tls.certresolver=letsencrypt"
      - "traefik.http.routers.dashboard.service=api@internal"
    cap_drop:
      - ALL
    cap_add:
      - CAP_NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    networks:
      proxy:
        ipv4_address: 172.22.1.254

volumes:
  letsencrypt:
    driver: local

networks:
  proxy:
    name: proxy
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: "1450" # match eth0 MTU; bridges default to 1500 and oversize packets get dropped at the 1450 hop
    ipam:
      config:
        - subnet: 172.22.1.0/24
          gateway: 172.22.1.1
