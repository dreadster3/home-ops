services:
  appraise:
    container_name: appraise
    image: caronc/apprise:v1.4.1@sha256:25e0577915c2f06233ae1dce077f05c0fc9ba4f0ea89de5aee18a32b2ee9a75c
    restart: unless-stopped
    environment:
      APPRISE_STATEFUL_MODE: simple
      APPRISE_WORKER_COUNT: 1
      APPRISE_ADMIN: y
    logging:
      driver: "json-file"
      options:
        max-size: "500m"
        max-file: "2"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.appraise.rule=Host(`appraise.${INTERNAL_DOMAIN}`)"
      - "traefik.http.routers.appraise.entrypoints=websecure"
      - "traefik.http.routers.appraise.tls.certresolver=letsencrypt"
      - "traefik.http.services.appraise.loadbalancer.server.port=8000"
    security_opt:
      - no-new-privileges:true
    volumes:
      - config:/config
      - plugins:/plugin
      - attachments:/attach
    networks:
      - proxy

volumes:
  config:
    driver: local
  plugins:
    driver: local
  attachments:
    driver: local

networks:
  proxy:
    external: true
