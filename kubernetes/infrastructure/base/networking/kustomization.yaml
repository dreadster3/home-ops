apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/subcategory: networking

components:
  - ../../../components/substitute

resources:
  - namespace.yaml
  - envoy-gateway
  - cilium
