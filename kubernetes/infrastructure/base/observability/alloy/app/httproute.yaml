apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy-otlp
spec:
  parentRefs:
    - name: envoy-internal
      namespace: envoy
  hostnames:
    - otlp.${INTERNAL_DOMAIN}
  rules:
    - backendRefs:
        - name: alloy
          port: 4318
