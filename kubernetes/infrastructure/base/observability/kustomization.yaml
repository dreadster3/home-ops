apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/subcategory: observability

resources:
  - namespace.yaml
  - metrics-server
  - victoria-logs
  - prometheus
  - victoria-metrics
  - alloy
  - grafana
  - krr
