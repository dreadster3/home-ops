apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../base/platform/flux/instance

patches:
  - target:
      kind: HelmRelease
      name: flux-instance
    path: helm.yaml
