apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: networking

labels:
  - pairs:
      app.kubernetes.io/subcategory: networking

components:
  - ../../../../components/substitute

resources:
  - ../../../base/networking/namespace.yaml
  - envoy-gateway
