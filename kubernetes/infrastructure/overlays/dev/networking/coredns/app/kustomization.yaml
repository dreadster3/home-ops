apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../base/networking/coredns/app

patches:
  - target:
      kind: HelmRelease
      name: coredns
    path: helm.yaml
