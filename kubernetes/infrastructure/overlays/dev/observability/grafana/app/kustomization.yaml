apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../base/observability/grafana/app

patches:
  - path: helm.yaml
    target:
      kind: HelmRelease
      name: grafana
