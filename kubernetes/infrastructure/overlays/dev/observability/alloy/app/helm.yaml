apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
  namespace: monitoring
spec:
  values:
    alloy:
      resources:
        requests:
          cpu: 0m
          memory: 0Mi
    configReloader:
      resources:
        requests:
          cpu: 0m
          memory: 0Mi
