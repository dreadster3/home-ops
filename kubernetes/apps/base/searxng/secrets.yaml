apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: searxng-dragonfly
  namespace: searxng
spec:
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: searxng-dragonfly-credentials
    creationPolicy: Owner
  data:
    - secretKey: DRAGONFLY_PASSWORD
      remoteRef:
        key: /searxng/DRAGONFLY_PASSWORD
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: searxng-secret-env
  namespace: searxng
spec:
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: searxng-secret-env
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        SEARXNG_SECRET: "{{ .token }}"
        SEARXNG_VALKEY_URL: "valkey://:{{.password}}@searxng-dragonfly:6379/0"
  data:
    - secretKey: token
      remoteRef:
        key: /searxng/SECRET_TOKEN
    - secretKey: password
      remoteRef:
        key: /searxng/DRAGONFLY_PASSWORD
