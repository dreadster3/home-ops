apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: litellm-github-copilot-tokens
  namespace: litellm
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 100Mi