apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: litellm
  namespace: litellm
spec:
  parentRefs:
    - name: envoy-internal
      namespace: envoy
  hostnames:
    - litellm.${INTERNAL_DOMAIN}
  rules:
    - backendRefs:
        - name: litellm
          port: 4000