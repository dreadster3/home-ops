apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
  namespace: cognee
spec:
  configuration:
    destinationPath: s3://cognee
    endpointURL: https://truenas.${INTERNAL_DOMAIN}:30292
    s3Credentials:
      accessKeyId:
        name: cognee-backup-s3-account
        key: ACCESS_KEY
      secretAccessKey:
        name: cognee-backup-s3-account
        key: SECRET_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
  retentionPolicy: "7d"