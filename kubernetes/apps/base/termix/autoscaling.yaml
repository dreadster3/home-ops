apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: termix
  namespace: termix
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: termix
  minReplicaCount: 0
  maxReplicaCount: 1
  cooldownPeriod: 300
  advanced:
    restoreToOriginalReplicaCount: false
  triggers:
    - type: cron
      metadata:
        timezone: Etc/UTC
        start: "0 7 * * *"
        end: "0 23 * * *"
        desiredReplicas: "1"
