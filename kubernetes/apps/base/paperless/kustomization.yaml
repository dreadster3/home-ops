apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: &app paperless

labels:
  - pairs:
      app.kubernetes.io/module: *app

resources:
  - namespace.yaml
  - store.yaml
  - pgcluster.yaml
  - redis.yaml
  - secrets.yaml
  - helm.yaml
  - network.yaml
