apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
  namespace: n8n
spec:
  configuration:
    destinationPath: s3://n8n
    endpointURL: https://truenas.${INTERNAL_DOMAIN}:30292
    s3Credentials:
      accessKeyId:
        name: n8n-backup-s3-account
        key: ACCESS_KEY
      secretAccessKey:
        name: n8n-backup-s3-account
        key: SECRET_KEY
    wal:
      compression: gzip
    data:
      compression: gzip
  retentionPolicy: "7d"
