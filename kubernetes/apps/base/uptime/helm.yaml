apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: uptime
  namespace: uptime
spec:
  interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  chart:
    spec:
      chart: app-template
      version: 5.1.x
      interval: 24h
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  values:
    controllers:
      main:
        annotations:
          reloader.stakater.com/auto: "true"
        initContainers:
          litestream-restore:
            image:
              repository: litestream/litestream
              tag: 0.5.17@sha256:4b02b9859a6b6b4087d8b8944e15f7e984bd7957cba322bbeee38b0e27b9656a
            args:
              - restore
              - -if-db-not-exists
              - -if-replica-exists
              - /app/data/kuma.db
            env:
              TZ: ${TIMEZONE}
            envFrom:
              - secretRef:
                  name: litestream-backup-credentials
            resources:
              requests:
                cpu: 15m
                memory: &memory 50Mi
              limits:
                memory: *memory
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
        containers:
          main:
            image:
              repository: docker.io/louislam/uptime-kuma
              tag: 2.5.3@sha256:3e24e96c89efff0e3a4b0698cbdd36c15ad3022371db57166e5588853002ee5c
            env:
              TZ: ${TIMEZONE}
              UPTIME_KUMA_DB_TYPE: sqlite
            ports:
              - containerPort: 3001
                name: http
                protocol: TCP
            resources:
              requests:
                cpu: 70m
                memory: 256Mi
              limits:
                memory: 256Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
          litestream:
            image:
              repository: litestream/litestream
              tag: 0.5.17@sha256:4b02b9859a6b6b4087d8b8944e15f7e984bd7957cba322bbeee38b0e27b9656a
            args: [replicate]
            env:
              TZ: ${TIMEZONE}
            envFrom:
              - secretRef:
                  name: litestream-backup-credentials
            resources:
              requests:
                cpu: 15m
                memory: &memory 100Mi
              limits:
                memory: *memory
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop: ["ALL"]
        pod:
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            seccompProfile: { type: RuntimeDefault }

    service:
      main:
        controller: main
        ports:
          http:
            port: 80
            targetPort: http

    route:
      main:
        hostnames:
          - uptime.${INTERNAL_DOMAIN}
        parentRefs:
          - name: envoy-internal
            namespace: envoy
        rules:
          - backendRefs:
              - identifier: main
                port: http

    persistence:
      data:
        enabled: true
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        storageClass: ceph-block
        size: 10Gi
        globalMounts:
          - path: /app/data
      litestream:
        enabled: true
        type: configMap
        name: litestream-config
        advancedMounts:
          main:
            litestream-restore:
              - path: /etc/litestream.yml
                readOnly: true
                subPath: litestream.yml
            litestream:
              - path: /etc/litestream.yml
                readOnly: true
                subPath: litestream.yml
