apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: &app openwebui

labels:
  - pairs:
      app.kubernetes.io/module: *app

resources:
  - namespace.yaml
  - store.yaml
  - pgcluster.yaml
  - secrets.yaml
  - redis.yaml
  # - ollama.yaml
  - openwebui.yaml
  - network.yaml
