apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: paperless-cluster-bucket
  namespace: paperless
spec:
  bucketName: paperless-cluster
  storageClassName: ceph-bucket
  additionalConfig:
    maxSize: 1G
