apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: sure-cluster-bucket
  namespace: sure
spec:
  bucketName: sure-cluster
  storageClassName: ceph-bucket
  additionalConfig:
    maxSize: 1G