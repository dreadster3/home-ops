apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: n8n
  namespace: n8n
spec:
  values:
    controllers:
      main:
        containers:
          main:
            resources:
              requests:
                cpu: 0m
                memory: 0Mi
