apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
  namespace: n8n
spec:
  configuration:
    destinationPath: s3://n8n-cluster
    endpointURL: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local
    s3Credentials:
      accessKeyId:
        name: n8n-cluster-bucket
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: n8n-cluster-bucket
        key: AWS_SECRET_ACCESS_KEY
