apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: authentik
  namespace: authentik
spec:
  values:
    server:
      replicas: 1
      resources:
        requests:
          cpu: 0m
          memory: 0Mi
    worker:
      replicas: 1
      resources:
        requests:
          cpu: 0m
          memory: 0Mi
