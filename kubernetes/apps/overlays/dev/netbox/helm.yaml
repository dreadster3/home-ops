apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: netbox
  namespace: netbox
spec:
  values:
    resources:
      requests:
        cpu: 0m
        memory: 0Mi
    worker:
      resources:
        requests:
          cpu: 0m
          memory: 0Mi
