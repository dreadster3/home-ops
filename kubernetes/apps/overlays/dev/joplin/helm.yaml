apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: joplin
  namespace: joplin
spec:
  values:
    controllers:
      main:
        containers:
          main:
            resources:
              requests:
                cpu: 0m
                memory: 0Mi
