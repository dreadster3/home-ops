apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: joplin

labels:
  - pairs:
      app.kubernetes.io/module: joplin

resources:
  - ../../../base/joplin
  - bucket.yaml

patches:
  - path: helm.yaml
    target:
      kind: HelmRelease
  - path: pgcluster.yaml
    target:
      kind: Cluster
  - path: store.yaml
    target:
      kind: ObjectStore
