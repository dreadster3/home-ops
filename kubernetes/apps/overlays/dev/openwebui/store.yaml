apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
  namespace: openwebui
spec:
  configuration:
    destinationPath: s3://openwebui-cluster
    endpointURL: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local
    s3Credentials:
      accessKeyId:
        name: openwebui-cluster-bucket
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: openwebui-cluster-bucket
        key: AWS_SECRET_ACCESS_KEY
