apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: openwebui-cluster-bucket
  namespace: openwebui
spec:
  bucketName: openwebui-cluster
  storageClassName: ceph-bucket
  additionalConfig:
    maxSize: 1G
