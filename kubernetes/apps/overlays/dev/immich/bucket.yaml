apiVersion: objectbucket.io/v1alpha1
kind: ObjectBucketClaim
metadata:
  name: immich-cluster-bucket
  namespace: immich
spec:
  bucketName: immich-cluster
  storageClassName: ceph-bucket
  additionalConfig:
    maxSize: 1G