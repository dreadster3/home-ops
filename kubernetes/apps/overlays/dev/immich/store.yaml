apiVersion: barmancloud.cnpg.io/v1
kind: ObjectStore
metadata:
  name: backup-store
  namespace: immich
spec:
  configuration:
    destinationPath: s3://immich-cluster
    endpointURL: http://rook-ceph-rgw-ceph-objectstore.rook-ceph.svc.cluster.local
    s3Credentials:
      accessKeyId:
        name: immich-cluster-bucket
        key: AWS_ACCESS_KEY_ID
      secretAccessKey:
        name: immich-cluster-bucket
        key: AWS_SECRET_ACCESS_KEY